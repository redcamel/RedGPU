/**
 * [KO] 3D 및 2D 객체를 표현하는 기본 단위인 Mesh와 관련 핵심 클래스들을 제공합니다.
 * [EN] Provides Mesh, the basic unit for representing 3D and 2D objects, along with related core classes.
 *
 * [KO] 지오메트리, 머티리얼, 트랜스폼 정보를 결합하여 실제 렌더링되는 객체를 구성하며, LOD(Level of Detail) 관리 기능도 포함합니다.
 * [EN] It combines geometry, material, and transform information to construct actual rendered objects, including LOD (Level of Detail) management features.
 *
 * @packageDocumentation
 */
import * as CoreMesh from "./core";
import Mesh from "./Mesh";

export {Mesh, CoreMesh};
